import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#ffffff'
    },
    secondary: {
      main: '#21B8F9',
    },
    error: {
      main: '#F33451',
    }
  },
});

export default theme